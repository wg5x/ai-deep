# Runtime

Runtime 是项目的执行环境。

## 定义

```text
项目内核 = 文档、原则、技能、流程、实验模板
运行时 = 负责读取内核并执行工作流的程序环境
```

当前运行时：

```text
Codex + 本地文件系统 + Git
```

未来运行时：

```text
Web + API + Worker + Scheduler + 数据存储 + Git + LLM
```

详细说明见：

- `architecture.md`
